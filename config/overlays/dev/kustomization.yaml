apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: service-router-system

namePrefix: service-router-operator-

resources:
- ../../default

patches:
- path: manager_dev_patch.yaml
  target:
    kind: Deployment
    name: controller-manager
