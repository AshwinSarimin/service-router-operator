apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - release.yaml

#configMapGenerator:
#  - name: external-dns-values
#    files:
#      - values.yaml
#    options:
#      disableNameSuffixHash: false
#
## Apply region suffix to resources (e.g., weu, neu)
#nameSuffix: -${DNS_REGION}
#
## Target namespace for Flux configuration resources
#namespace: ns-${ENV_LETTER}-all-addon-flux-cluster-config-01
#
#configurations:
#  - ./kustomizeconfig.yaml
#
## Validate that required variables are set
## DNS_REGION, ENV_LETTER must be provided via postBuild substitutions
